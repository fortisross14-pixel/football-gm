import React from 'react';
import { SectionTitle, Metric, Pill } from '../components/UI.jsx';
import { money } from '../game/engine.js';
export default function Finance({game}){const f=game.finance;const profit=f.revenue-f.costs;return <><SectionTitle eyebrow="Finanzas y negocio" title="Cuenta de gestión" action={<Pill tone={profit>=0?'positive':'negative'}>{profit>=0?'Superávit':'Déficit'} {money(profit)}</Pill>}/><div className="metric-strip"><Metric label="Caja" value={money(game.cash)}/><Metric label="Ingresos temporada" value={money(f.revenue)}/><Metric label="Costes temporada" value={money(f.costs)}/><Metric label="Resultado" value={money(profit)} tone={profit>=0?'positive':'negative'}/></div><div className="finance-grid"><div className="panel"><div className="panel-head"><div><span>INGRESOS</span><h3>De dónde entra el dinero</h3></div></div>{[['Entradas',f.ticket],['Merchandising',f.merch],['Patrocinios',f.sponsor],['Hospitality & restauración',f.hospitality]].map(([l,v])=><div className="ledger" key={l}><span>{l}</span><b>{money(v)}</b></div>)}</div><div className="panel"><div className="panel-head"><div><span>COSTES</span><h3>Dónde se va el dinero</h3></div></div>{[['Salarios',f.wages],['Estadio e instalaciones',f.facilities],['Fichajes',f.transfersIn]].map(([l,v])=><div className="ledger" key={l}><span>{l}</span><b>{money(v)}</b></div>)}</div></div></>}
