import React from 'react';
import { SectionTitle, Bar, Pill } from '../components/UI.jsx';
import { money } from '../game/engine.js';
export default function Coach({game,setGame,hire}){const coach=game.staff.coach; return <><SectionTitle eyebrow="Área deportiva" title="Entrenador y modelo de juego"/><div className="mode-switch"><button className={game.controlMode==='coach'?'selected':''} onClick={()=>setGame({...game,controlMode:'coach'})}><b>Contratar entrenador</b><span>El técnico decide el estilo. Tú gestionas encaje, contrato y resultados.</span></button><button className={game.controlMode==='self'?'selected':''} onClick={()=>setGame({...game,controlMode:'self'})}><b>Control total</b><span>Tú asumes el banquillo. La calidad depende de tu conocimiento táctico ({game.gm.stats.tactics}/10).</span></button></div>{game.controlMode==='coach'&&<><div className="panel coach-current"><div><span>ENTRENADOR ACTUAL</span><h3>{coach.name}</h3><p>{coach.style.name} · {coach.trait}</p></div><div><Bar label="Calidad" value={coach.rating}/><Bar label="Encaje cultural" value={coach.style.id===game.fanStyle?92:61}/><small>La afición del club prefiere: <b>{game.fanStyle==='vertical'?'juego vertical':game.fanStyle==='press'?'presión alta':'control con balón'}</b></small></div></div><h3 className="minor-title">Mercado de entrenadores</h3><div className="card-grid">{game.coachCandidates.map(c=><article className="person-card" key={c.id}><div className="avatar">{c.name.split(' ').map(x=>x[0]).slice(0,2).join('')}</div><h4>{c.name}</h4><Pill>{c.trait}</Pill><p>{c.style.name}</p><Bar value={c.rating} label="Rating"/><div className="card-foot"><span>{money(c.salary)}/año</span><button onClick={()=>hire('Entrenador',c)}>Contratar</button></div></article>)}</div></>}</>}
