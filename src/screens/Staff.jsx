import React from 'react';
import { SectionTitle, Pill, Bar } from '../components/UI.jsx';
import { money } from '../game/engine.js';
const roles=[['sportingDirector','Director deportivo'],['cmo','Chief Merchandising Officer'],['cfo','CFO'],['scout','Jefe de scouting']];
export default function Staff({game,hire}){return <><SectionTitle eyebrow="Club y directiva" title="Organigrama profesional"/><div className="staff-slots">{roles.map(([key,label])=><article className="staff-slot" key={key}><span>{label.toUpperCase()}</span>{game.staff[key]?<><h3>{game.staff[key].name}</h3><Pill>{game.staff[key].trait}</Pill><Bar label="Rating" value={game.staff[key].rating}/><small>{money(game.staff[key].salary)}/año</small></>:<><h3>Vacante</h3><p>Ahorras salario, pero tú absorbes esta función y pierdes eficiencia especializada.</p></>}</article>)}</div><h3 className="minor-title">Candidatos disponibles</h3>{roles.map(([key,label])=>!game.staff[key]&&<div className="candidate-row" key={key}><div className="candidate-role"><b>{label}</b><span>{label==='Chief Merchandising Officer'?'Mejora demanda, previsión y coste de fabricación.':label==='CFO'?'Mejora disciplina financiera y reporting.':label==='Director deportivo'?'Mejora mercado, planificación y talento.':'Amplía información y calidad del scouting.'}</span></div>{game.staffCandidates[label].slice(0,3).map(c=><article key={c.id}><div><b>{c.name}</b><span>{c.trait} · Rating {c.rating}</span></div><span>{money(c.salary)}/año</span><button onClick={()=>hire(label,c)}>Contratar</button></article>)}</div>)}</>}
