import React from 'react';
import { SectionTitle, Pill } from '../components/UI.jsx';
import { money } from '../game/engine.js';
export default function Sponsors({game,accept,negotiate}){return <><SectionTitle eyebrow="Finanzas y negocio" title="Patrocinadores"/><div className="sponsor-current">{game.sponsors.length===0?<div className="empty">Todavía no has firmado acuerdos. En un club pequeño, el primer patrocinador puede ser la diferencia entre invertir o sobrevivir.</div>:game.sponsors.map(s=><article key={s.id}><div className="sponsor-logo">{s.brand.slice(0,2).toUpperCase()}</div><div><b>{s.brand}</b><span>{s.type}</span></div><strong>{money(s.annual)}/año</strong></article>)}</div><h3 className="minor-title">Ofertas sobre la mesa</h3><div className="sponsor-grid">{game.sponsorOffers.map(s=><article className="sponsor-offer" key={s.id}><div className="sponsor-logo">{s.brand.slice(0,2).toUpperCase()}</div><h3>{s.brand}</h3><Pill>{s.type}</Pill><strong>{money(s.annual)}<small>/año</small></strong><div className="offer-kpis"><div><span>Duración</span><b>{s.years} años</b></div><div><span>Bonus victoria</span><b>{money(s.bonusWin)}</b></div><div><span>Encaje</span><b>{s.fit}%</b></div></div><div className="button-row"><button disabled={s.negotiated} onClick={()=>negotiate(s)}>{s.negotiated?'Negociado':'Negociar'}</button><button className="primary" onClick={()=>accept(s)}>Firmar</button></div></article>)}</div></>}
