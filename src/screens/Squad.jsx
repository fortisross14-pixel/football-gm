import React,{useMemo,useState} from 'react';
import { SectionTitle, SortHeader, Pill } from '../components/UI.jsx';
import { money } from '../game/engine.js';
export default function Squad({game}){const [sort,setSort]=useState({key:'overall',dir:'desc'}); const rows=useMemo(()=>[...game.players].sort((a,b)=>{const av=a[sort.key],bv=b[sort.key];return (typeof av==='string'?av.localeCompare(bv):av-bv)*(sort.dir==='asc'?1:-1)}),[game.players,sort]); const onSort=(key)=>setSort({key,dir:sort.key===key&&sort.dir==='desc'?'asc':'desc'}); return <><SectionTitle eyebrow="Área deportiva" title="Plantilla" action={<Pill>{game.players.length}/30 fichas</Pill>}/><div className="panel table-panel"><table><thead><tr><th><SortHeader label="Jugador" sortKey="name" current={sort} onSort={onSort}/></th><th><SortHeader label="Pos" sortKey="pos" current={sort} onSort={onSort}/></th><th><SortHeader label="Edad" sortKey="age" current={sort} onSort={onSort}/></th><th><SortHeader label="OVR" sortKey="overall" current={sort} onSort={onSort}/></th><th><SortHeader label="Techo" sortKey="potential" current={sort} onSort={onSort}/></th><th><SortHeader label="Valor" sortKey="value" current={sort} onSort={onSort}/></th><th><SortHeader label="Salario" sortKey="salary" current={sort} onSort={onSort}/></th><th><SortHeader label="Moral" sortKey="morale" current={sort} onSort={onSort}/></th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td><b>{p.name}</b><small>{p.contract} años contrato</small></td><td><Pill>{p.pos}</Pill></td><td>{p.age}</td><td><strong className="rating">{p.overall}</strong></td><td>{p.potential}</td><td>{money(p.value)}</td><td>{money(p.salary)}</td><td>{p.morale}%</td></tr>)}</tbody></table></div></>}
